# Correlation terminology

NETRA implements weighted shared-indicator campaign correlation and an investigation graph visualization. Sender, domain and observed public IP relationships are evidence links. Current campaign decisions use weighted indicators, not graph embeddings or a trained graph model. Shared infrastructure does not establish a shared attacker or the human sender's identity. Historical audit wording "graph correlation" refers to these evidence relationships; this document defines the current scope for presentations and documentation.
